# Queries to fetch cart & wishlist from the system.
type Query {
  # Fetches the current user's cart.
  getCart: GetCartOrWishListResponseOrError!

  # Fetches the current user's wishlist.
  getWishlist: GetCartOrWishListResponseOrError!
}
