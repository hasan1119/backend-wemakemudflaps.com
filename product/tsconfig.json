{
  /**
   * TypeScript compiler configuration for the project.
   *
   * Workflow:
   * 1. Configures the target JavaScript version and module system.
   * 2. Enables features like strict mode, decorator support, and JSON module resolution.
   * 3. Specifies input and output directories for compilation.
   * 4. Defines included files and type roots for type checking.
   */
  "compilerOptions": {
    "target": "ES6",
    "module": "commonjs",
    "strict": false,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "typeRoots": ["./node_modules/@types"],
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "lib": ["ES2017", "ES2018", "DOM"]
  },
  "include": ["src/**/*", "src/utils", "utils", "context.ts"]
}
